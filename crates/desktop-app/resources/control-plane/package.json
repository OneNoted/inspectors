{
  "name": "@acu/control-plane",
  "version": "0.1.0-alpha.3",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "start": "node dist/index.js",
    "test": "bun run build && node --test dist/**/*.test.js"
  },
  "dependencies": {
    "playwright-core": "^1.55.0"
  }
}
