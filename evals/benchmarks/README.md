# Benchmarks

The MVP ships with JSON task definitions under `evals/tasks/` and a smoke benchmark runner in `scripts/run-smoke-eval.py`.

Metrics currently captured:
- success
- duration_ms
- step_count
- human_intervention
- artifact_path
